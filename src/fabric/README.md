# Fabric Workspace Items

Place exported Fabric workspace items here after connecting the Fabric workspace to Git.

Expected examples:

- Lakehouse item folder
- Notebook item folder
- Data Pipeline item folder
- Semantic model item folder
- Power BI report item folder
- Dataflow Gen2 item folder

Fabric Git integration exports supported workspace item definitions/metadata. It does not export the actual Lakehouse data rows.
